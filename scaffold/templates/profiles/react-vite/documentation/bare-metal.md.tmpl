The React/Vite profile does not generate a bare-metal installer. Deploy the
immutable Nginx container, or have the application developer define and test a
separate build, web-server, atomic-release, rollback, and service-management
procedure before claiming bare-metal support.
